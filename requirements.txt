# ── Core dashboard & server ───────────────────────────
dash==2.14.2
dash-bootstrap-components==1.4.1
Flask==2.3.3
gunicorn==21.2.0             # App Engine entrypoint: gunicorn -b :8080 app:server

# ── Data handling & analytics ─────────────────────────
pandas==2.1.4
numpy==1.24.4
scipy==1.11.4
scikit-learn==1.3.2          # Random-Survival-Forest, clustering, etc.
lifelines==0.27.8            # Kaplan-Meier & log-rank tests
tensorflow==2.15.0           # LSTM model saved as LSTM_model.keras
keras==2.15.0                # (included with tf-2.15, but explicit for clarity)

# ── Visualization ─────────────────────────────────────
plotly==5.18.0
kaleido==0.2.1               # Static image export for Plotly figs
matplotlib==3.8.2
seaborn==0.13.1

# ── Geospatial / mapping ──────────────────────────────
folium==0.15.1               # Interactive leaflet maps in notebooks

# ── Utilities ─────────────────────────────────────────
joblib==1.3.2                # Model serialization
google-cloud-storage==2.13.0 # For GCS bucket access via BUCKET_NAME env var

# ── Pin the major Python version used on App Engine ───
# (GAE pulls system runtime, but this helps when recreating locally)
python_version == "3.10.*"
